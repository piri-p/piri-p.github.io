---
title: "Argilla"
date: 2026-07-09
description: ""
tags: [""]
---

# Argilla

## Setup

Self-host with [Docker Compose](https://docs.argilla.io/v2.0/getting_started/how-to-deploy-argilla-with-docker/)

```bash
curl https://raw.githubusercontent.com/argilla-io/argilla/main/examples/deployments/docker/docker-compose.yaml -o docker-compose.yaml
sudo docker compose up -d
```

Inside the docker-compose.yml contains default username, password, and API key which can be used for initial login.
This may require the step of removing the corrupted `-XX:UseSVE=0` flag from docker-compose.yml.

### Common issues with Steup

**Service doesn't start**

Check service status

```bash
sudo docker compose ps -a
```

If some service doesn't start (e.g. Elasticsearch), take service name and check their logs

```bash
sudo docker logs argilla-elasticsearch-1 --tail=100    # TODO may need change to service name
```

If the logs show

```bash
Unrecognized VM option 'UseSVE=0'
Error: Could not create the Java Virtual Machine.
Error: A fatal exception has occurred. Program will exit.
```

Remove  `-XX:UseSVE=0` flag from docker-compose.yml.
